"""
Функция lists_sum принимает произвольное количество списков чисел и возвращает сумму всех этих чисел.

Аргумент unique по умолчанию равен False. Если unique=True, функция вернет сумму всех уникальных чисел из всех списков.
Если списки пустые или не поданы в функцию, сумма чисел считается равной нулю.
"""
def lists_sum(*args,unique=False):
    K=[] #объявлять нужно внутри функции!
    s=[*args]
    for i in s:
        K=K+[*i]
    #print(s)
    if unique==True:
        S=set(K)
        return sum(S)
    else: return sum(K)
